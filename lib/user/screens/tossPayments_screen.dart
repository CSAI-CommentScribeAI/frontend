import 'package:flutter/material.dart';

class TosspaymentsPage extends StatelessWidget {
  const TosspaymentsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('토스페이먼츠'),
      ),
    );
  }
}
